<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.maxkb4j.application.mapper.ApplicationChatUserStatsMapper">

    <select id="getCustomerCountTrend" resultType="com.maxkb4j.application.vo.ApplicationStatisticsVO">
        SELECT
        COUNT ( id ) AS "customer_added_count",
        create_time :: DATE as "day"
        FROM
        application_chat_user_stats
        <where>
            APPLICATION_ID = #{appId}
            <if test="query.startTime != null and query.startTime != ''">
                and  <![CDATA[TO_CHAR(create_time, 'YYYY-MM-DD') >= #{query.startTime} ]]>
            </if>
            <if test="query.endTime != null and query.endTime != ''">
                and  <![CDATA[TO_CHAR(create_time, 'YYYY-MM-DD') <= #{query.endTime} ]]>
            </if>
        </where>
        GROUP BY "day";
    </select>

    <insert id="insertIfAbsent">
        INSERT INTO application_chat_user_stats (id, access_num, intra_day_access_num, application_id,
                                                 chat_user_id, chat_user_type, create_time, update_time)
        VALUES (#{id}, #{accessNum}, #{intraDayAccessNum}, #{applicationId},
                #{chatUserId}, #{chatUserType}, NOW(), NOW())
        ON CONFLICT (chat_user_id, application_id) DO NOTHING
    </insert>

    <update id="incrementAccessNum">
        UPDATE application_chat_user_stats
        SET access_num           = access_num + 1,
            intra_day_access_num = intra_day_access_num + 1,
            update_time          = NOW()
        WHERE chat_user_id = #{chatUserId}
          AND application_id = #{applicationId}
    </update>

</mapper>
