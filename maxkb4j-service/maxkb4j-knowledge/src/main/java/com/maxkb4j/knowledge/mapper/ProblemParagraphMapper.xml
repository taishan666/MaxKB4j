<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.maxkb4j.knowledge.mapper.ProblemParagraphMapper">

    <select id="getProblems" resultType="com.maxkb4j.knowledge.vo.ProblemParagraphVO">
        SELECT
        ppm.ID,
        P."content",
        ppm.knowledge_id,
        ppm.document_id,
        ppm.paragraph_id,
        ppm.problem_id
        FROM
        problem_paragraph_mapping ppm
        LEFT JOIN problem P ON P.ID = ppm.problem_id
        <where>
            <if test="knowledgeId != null and knowledgeId != ''">
                AND ppm.knowledge_id =   #{knowledgeId}
            </if>
            <if test="docIds != null and docIds.size() > 0">
                AND ppm.document_id IN
                <foreach item="docId" collection="docIds" open="(" separator="," close=")">
                    #{docId}
                </foreach>
            </if>
        </where>
    </select>
    <select id="getProblemsByParagraphIds" resultType="com.maxkb4j.knowledge.entity.ProblemEntity">
        SELECT
            P.ID,
            P."content"
        FROM
            problem_paragraph_mapping ppm
                INNER  JOIN problem P ON P.ID = ppm.problem_id
        WHERE ppm.paragraph_id IN
        <foreach item="paragraphId" collection="paragraphIds" open="(" separator="," close=")">
            #{paragraphId}
        </foreach>
    </select>
    <select id="getActivePPbyProblemIds" resultType="com.maxkb4j.knowledge.entity.ProblemParagraphEntity">
        SELECT
            ppm.knowledge_id,
            ppm.paragraph_id,
            ppm.problem_id
        FROM
            problem_paragraph_mapping ppm
        LEFT JOIN paragraph P ON P.ID = ppm.paragraph_id
        WHERE P.is_active = true
        AND ppm.problem_id IN
        <foreach item="problemId" collection="problemIds" open="(" separator="," close=")">
            #{problemId}
        </foreach>
    </select>

</mapper>
