<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.maxkb4j.knowledge.mapper.TagMapper">


    <select id="listTags" resultType="com.maxkb4j.knowledge.vo.TagVO">
        SELECT
        tag.id,
        tag.key,
        tag.value,
        COUNT(dt.tag_id) AS doc_count
        FROM TAG tag
        LEFT JOIN DOCUMENT_TAG dt ON tag.id = dt.tag_id
        WHERE tag.knowledge_id = #{knowledgeId}
        <if test="name != null and name != ''">
            AND CONCAT(tag.key, tag.value) LIKE CONCAT('%', #{name}, '%')
        </if>
        GROUP BY
        tag.id,
        tag.key,
        tag.value
    </select>
</mapper>
