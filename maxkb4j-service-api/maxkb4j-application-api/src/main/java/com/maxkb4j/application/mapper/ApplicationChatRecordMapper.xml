<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.maxkb4j.application.mapper.ApplicationChatRecordMapper">

    <select id="listByAppIdAndChatUserId" resultType="com.maxkb4j.application.entity.ApplicationChatRecordEntity">
        SELECT record.problem_text, record.answer_text
        FROM application_chat chat
                 LEFT JOIN application_chat_record record
                           ON chat.id = record.chat_id
        WHERE chat.application_id = #{applicationId}
          AND chat.chat_user_id = #{chatUserId}
        ORDER BY record.create_time DESC
        LIMIT #{pageSize} OFFSET #{offset};
    </select>
    <select id="countByAppIdAndChatUserId" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM application_chat chat
                 LEFT JOIN application_chat_record record
                           ON chat.id = record.chat_id
        WHERE chat.application_id = #{applicationId}
          AND chat.chat_user_id = #{chatUserId}
        ORDER BY record.create_time DESC
    </select>

</mapper>
