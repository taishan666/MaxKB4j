<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.maxkb4j.knowledge.mapper.GraphEntityMapper">

    <select id="searchByNames" resultType="com.maxkb4j.knowledge.entity.GraphEntityEntity">
        SELECT id, name, entity_type, description, knowledge_id, document_id, is_active
        FROM graph_entity
        WHERE is_active = true
        AND knowledge_id = #{knowledgeId}
        AND name IN
        <foreach item="name" collection="names" open="(" separator="," close=")">
            #{name}
        </foreach>
    </select>

    <select id="searchByNameLike" resultType="com.maxkb4j.knowledge.entity.GraphEntityEntity">
        SELECT id, name, entity_type, description, knowledge_id, document_id, is_active
        FROM graph_entity
        WHERE is_active = true
        AND knowledge_id = #{knowledgeId}
        AND name LIKE CONCAT('%', #{keyword}, '%')
    </select>

</mapper>