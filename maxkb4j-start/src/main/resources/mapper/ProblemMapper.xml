<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.maxkb4j.knowledge.mapper.ProblemMapper">

    <select id="pageByDatasetId" resultType="com.maxkb4j.knowledge.vo.ProblemVO">
        SELECT
            p.*,
            COUNT(ppm.paragraph_id) AS paragraph_count
        FROM
            problem p
                LEFT JOIN
            problem_paragraph_mapping ppm ON p.id = ppm.problem_id
        WHERE p.KNOWLEDGE_ID = #{knowledgeId}
        <if test="content != null and content != ''">
            and p.content like CONCAT('%',#{content},'%')
        </if>
        GROUP BY
            p.id
        ORDER BY
            p.create_time DESC
    </select>

</mapper>
